// 밖에서 불러오는 애들 
let img1, img2, img3,img4;
let myFont;
//글자 관련 변수들. 
let textPage; // 벽돌에 입힐 이미지
let textPageMain; // 문에 입힐 이미지 
let name1; // 입력받은  텍스트 저장 
let textFlag=0;  // 이름을 입력받았을 때. 
// 카메라 이동 변수들 
let keyX=0;
let keyY=0;
let keyZ=250;
let val = keyZ+900;
// 스페이스바 눌렀는지 확인 
let colorPlag=0;
// 문 있는지 확인 
let doorFlag=0;
// 버튼 관련 
let buttonFlag = 0;  // 버튼 있는지
let input, button, greeting;
let button1;

